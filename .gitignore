.DS_Store
.jack
.env
.env.*
!.env.example

# Editors
.vscode-test

# Node
node_modules/
dist/
*.tsbuildinfo
*.tgz
.yarn-integrity

# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Go / CLI build output
packages/cli/ryskV12-*
# `go build ./...` inside packages/cli emits a binary named after the dir
/packages/cli/cli

# CLI binaries dropped into the SDK packages by `make dev-bin`
# (packages/py/ryskV12 is the python package itself, hence the exact names)
/packages/ts/ryskV12
/packages/py/ryskV12cli
